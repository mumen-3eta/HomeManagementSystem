const registerUserSchema = require('./registerUserSchema');
const loginSchema = require('./loginSchema');
const profileDataSchema = require('./profileDataSchema');

module.exports = {
  registerUserSchema,
  loginSchema,
  profileDataSchema,
};
